# ==============================================================
# requirements.txt — Conflict-free for Python 3.13
#
# CLEAN INSTALL COMMANDS:
#   pip uninstall langchain langchain-core langchain-openai langchain-community langchain-text-splitters langsmith chromadb openai httpx pydantic fastapi uvicorn -y
#   pip install -r requirements.txt
#   pip check
# ==============================================================


# ── Web framework ──────────────────────────────────────────────
# fastapi 0.115.x is the latest stable, fully supports pydantic v2
fastapi>=0.115.0
uvicorn[standard]>=0.29.0


# ── Azure SDKs ─────────────────────────────────────────────────
azure-ai-documentintelligence==1.0.0b4
azure-core>=1.30.0


# ── OpenAI ─────────────────────────────────────────────────────
# Pinned to what you already have and works — do NOT go to 2.x
openai==1.109.1
httpx>=0.27.0,<0.28.0


# ── Pydantic ───────────────────────────────────────────────────
# Let pip pick the right version — all packages above need >=2.7.4,<3.0.0
# DO NOT hardpin pydantic — let pip resolve it
pydantic>=2.7.4,<3.0.0


# ── LangChain stack ─────────────────────────────────────────────
# All from the 0.3.x generation — must stay in sync
langchain-core==0.3.59
langchain==0.3.25
langchain-openai==1.1.9
langchain-community==0.3.23
langchain-text-splitters==0.3.8


# ── Vector store ────────────────────────────────────────────────
chromadb>=0.5.0


# ── Auth ────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4


# ── Utilities ───────────────────────────────────────────────────
python-dotenv>=1.0.0
regex>=2024.4.28
tiktoken>=0.7.0
